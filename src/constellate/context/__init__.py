"""Context plane (ADR 0012): LLM drivers, an in-process MCP agent loop, and a
fixed task suite that scores tool-call fidelity. Demonstration only — never
citable alongside the benchmark harness (ADR 0011); artifacts live in
`bench/context/`, outside `bench/report.py`'s glob on purpose.
"""
