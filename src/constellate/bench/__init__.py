"""Benchmark harness (phase 04): quality ablation, fusion tuning, flows,
open-loop latency. Logic lives here so mypy --strict and unit tests cover it;
`bench/` holds CLIs and committed results artifacts (same split as
`bench/probes.py` / `constellate.ingest.probes`).
"""
