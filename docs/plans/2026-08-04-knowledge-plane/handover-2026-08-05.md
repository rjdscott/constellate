# Handover — 2026-08-05, end of day

All nine planned phases 🟢 (see the [status table](README.md)). Main at
`b29ec2f`+, tree clean, CI green, 24 PRs merged. No engineering in
flight. This doc is the resume point for the next session.

## Decisions taken today (recorded, not yet acted on)

- **Talk target: conference slot, 30–45 min.** Tight arc, 2–3 live
  demos maximum, hard fallback discipline (snapshot mode for the UI,
  local driver as the offline LLM fallback).
- **Publication form: blog post series.** Each thread in
  [research 13 § Publication threads](../../research/2026-08-04-knowledge-plane-foundations/13-context-plane-llm.md)
  becomes a standalone post; the talk links to them.

## Next-step ideas, ranked (recommendation from the phase-09 close)

1. **Talk assembly as the spine** — outline + slide skeleton + demo
   script from `04-migration-narrative.md`, the findings docs
   (07/08/10/12/13), and lessons L1–L16. The strongest through-line
   candidate: *six consecutive adversarial reviews found their top
   defect in the proof machinery, not the product* (L7, L10, L13, the
   report's filename sort, the digest-truncated grounding, the scorer
   accusing correct answers).
2. **Small-model size sweep** (~hours, feeds post series): qwen3:4b /
   llama3.2:3b / a 14B through the existing context suite — one CLI
   call per model — to locate the chaining-fidelity cliff precisely.
3. **Neo4j CE reference arm** (~half day): audience-calibration number
   against the four engines already identical to four decimals. Shape
   pre-agreed in the [plan README parked list](README.md).
4. **Long-tail probe set** (~half day): probes from non-genome items;
   could flip the svd-vs-neural verdict where SVD's fallback lives
   (L15). Gives the embedding story a second act if the talk wants one.
5. **Blog drafts** — first post candidate: "The demo looked perfect.
   The tool call was for the wrong movie." (research 13 thread 1).
6. **Eridanus design** — deferral trigger ("after the analysis
   completes") has fired; still recommend keeping it a closing slide
   unless the talk needs a build arc.

## Environment state at handover

- **Containers up (left on purpose):** `constellate-orion`,
  `constellate-hydra-{postgres,qdrant,memgraph}`. Engines hold svd-arm
  data; configs at svd defaults. `make down PLATFORM=<p>` to stop,
  `make up PLATFORM=<p>` to restart.
- **Stopped:** uvicorn (:8000), vite dev servers. Restart per
  [run-explorer](../../runbooks/run-explorer.md).
- **Ollama:** user-space at `~/.local/share/ollama/bin/ollama`, model
  qwen3:8b pulled. The `serve` process resisted a sandboxed kill and
  may still be running (idle cost ≈ zero); `pkill -f 'ollama serve'`
  from a normal shell if you want it gone. Restart per
  [run-context-demo](../../runbooks/run-context-demo.md).
- **Secrets:** `ANTHROPIC_API_KEY` in gitignored `.env` (verified
  ignored; read only at suite runtime).
- **Python env:** synced with `--extra context` (anthropic, dotenv).
  `uv sync` replaces the extras set — name every extra you need
  (`--extra context --extra neural`).

## Resume checklist

```sh
git pull --ff-only          # main
make check                  # 144 tests + doc-check green expected
cat docs/plans/2026-08-04-knowledge-plane/handover-2026-08-05.md
```

Then pick from the ranked list above — items 2–4 are pre-scoped in the
plan README's parked-experiments section; item 1 starts from the
migration narrative.
